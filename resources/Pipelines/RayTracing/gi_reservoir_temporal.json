{
    "name": "gi_reservoir_temporal_pso",
    "shaders": {
        "compute": { "filepath": "gi_reservoir_temporal.hlsl", "entry_point": "CSMain" }
    },
    "per_pass_resources": [
        { "binding": 0, "type": "sampled_texture" },
        { "binding": 1, "type": "sampled_texture" },
        { "binding": 2, "type": "sampled_texture" },
        { "binding": 3, "type": "sampled_texture" },
        { "binding": 4, "type": "storage_buffer" }
    ],
    "options": { "default": {} }
}
