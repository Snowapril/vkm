{
    "name": "gi_reservoir_resolve_pso",
    "shaders": {
        "compute": { "filepath": "gi_reservoir_resolve.hlsl", "entry_point": "CSMain" }
    },
    "per_pass_resources": [
        { "binding": 0, "type": "sampled_texture" },
        { "binding": 1, "type": "sampled_texture" },
        { "binding": 2, "type": "sampled_texture" },
        { "binding": 3, "type": "storage_buffer" },
        { "binding": 4, "type": "storage_buffer" }
    ],
    "options": { "default": {} }
}
