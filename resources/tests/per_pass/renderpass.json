{
    "name": "per_pass_resources_pso",
    "shaders": {
        "compute": { "filepath": "per_pass_resources.hlsl", "entry_point": "CSMain" }
    },
    "per_pass_resources": [
        { "binding": 0, "type": "uniform_buffer" },
        { "binding": 1, "type": "storage_buffer" }
    ],
    "options": {
        "default": {}
    }
}
